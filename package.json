{
  "name": "cluster-finder",
  "version": "0.0.3",
  "description": "a clusterfinder test",
  "author": "ryuzeke",
  "scripts": {
    "start": "yarn clear && parcel src/index.html --open",
    "build": "yarn clear && parcel build src/index.html --public-url ./",
    "release": "standard-version && yarn build",
    "clear": "rimraf ./dist",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "@tweenjs/tween.js": "^18.6.4",
    "pixi-text-input": "^1.0.6",
    "pixi.js": "^6.0.1"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "parcel-bundler": "^1.12.5",
    "parcel-plugin-json-url-loader": "^0.1.2",
    "parcel-plugin-static-files-copy": "^2.6.0",
    "rimraf": "^3.0.2",
    "standard-version": "^9.1.1",
    "typescript": "^4.2.3"
  }
}
